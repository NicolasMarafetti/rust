/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.rustlabs.com',
            'static.wikia.nocookie.net'
        ],
    },
}

module.exports = nextConfig
