import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Head from 'next/head'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Rust Resource Hub: Complete Guide to Items & Values',
  description: 'Discover comprehensive and up-to-date resources, tools, weapons, and more for the popular survival game, Rust. Navigate with ease and enhance your gameplay experience with detailed item values and visuals.'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <Head>
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
